# Anti-example: Invalid enum values
# This file is intentionally malformed - enum fields have invalid values
# Expected error: Status must be one of the allowed values

header:
  schema-version: "2.2.0"
  last-updated: "2025-01-15"
  last-reviewed: "2025-01-15"
  url: https://example.com/security-insights.yml

repository:
  url: https://github.com/example/example-repo
  status: "invalid-status"  # Invalid: not one of the allowed values
  accepts-change-request: true
  accepts-automated-change-request: true
  core-team:
    - name: Jane Doe
      primary: true
  license:
    url: https://github.com/example/example-repo/blob/main/LICENSE
    expression: MIT
  security:
    assessments:
      self:
        comment: Self-assessment completed
    tools:
      - name: Example Tool
        type: "invalid-type"  # Invalid: not one of fuzzing, container, secret, SCA, SAST, other
        integration:
          adhoc: true
          ci: true
          release: true
        results: {}

