# Anti-example: Invalid schema version format
# This file is intentionally malformed - schema-version doesn't match the required pattern
# Expected error: Schema version must match pattern ^[1-9]+\.[0-9]+\.[0-9]+$

header:
  schema-version: "2.0"  # Invalid: missing patch version
  last-updated: "2025-01-15"
  last-reviewed: "2025-01-15"
  url: https://example.com/security-insights.yml

project:
  name: Example Project
  administrators:
    - name: Jane Doe
      primary: true
  repositories:
    - name: example-repo
      url: https://github.com/example/example-repo
      comment: Main repository
  vulnerability-reporting:
    reports-accepted: true
    bug-bounty-available: false

